agent:
  name: DedupAgent
  functions:
    - name: merge_dedup
      description: Merge and deduplicate every sorted chunk into one combined digest.
      arguments:
        - name: chunks
          type: list
      returns:
        type: dict
