agent:
  name: SmokeAgent
  functions:
    - name: ping
      description: Return a deterministic smoke string.
      arguments:
        - name: name
          type: str
      returns:
        type: str
